#include "main.h"
/**
 * mul - a function that multiplies two integers
 *
 * @a: the first num to multiply
 * @b: the second num to mutiple against the first num
 *
 * Return: Always 0 (Success)
 */
int mul(int a, int b)
{
	return (a * b);
}
